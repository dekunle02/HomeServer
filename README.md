This is a simple weekend project.
Built with Django + React
It serves a series of photos through the day on a screen
The backend is hosted on a local raspberry pi and the frontend is hosted on an old tablet secured to a wall
All data is stored locally
